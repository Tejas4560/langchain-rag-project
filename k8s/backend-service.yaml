apiVersion: v1
kind: Service
metadata:
  name: rag-backend-service
  namespace: rag-app
  labels:
    app: rag-backend
spec:
  selector:
    app: rag-backend
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP