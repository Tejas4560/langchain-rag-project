apiVersion: v1
kind: Service
metadata:
  name: rag-frontend-service
  namespace: rag-app
  labels:
    app: rag-frontend
spec:
  selector:
    app: rag-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP